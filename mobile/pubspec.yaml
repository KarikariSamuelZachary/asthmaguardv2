name: mobile
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.2
  font_awesome_flutter: ^10.8.0
  intl: ^0.19.0 # Added intl package
  flutter_svg: ^2.0.10+1 # Added flutter_svg
  url_launcher: ^6.2.6 # Added url_launcher
  provider: ^6.1.2
  flutter_rating_bar: ^4.0.1
  just_audio: ^0.9.36
  youtube_player_iframe: ^4.0.4 # Added youtube_player_iframe for YouTube video support
  shared_preferences: ^2.2.2 # Added for persistent storage
  firebase_core: ^3.15.1  
  firebase_auth: ^5.6.2  
  flutter_blue_plus: ^1.35.5
  permission_handler: ^12.0.1
  app_settings: ^6.1.1
  geolocator: ^10.1.0
  share_plus: ^11.0.0
  pdf: ^3.11.3
  google_maps_flutter: ^2.5.0
  flutter_local_notifications: ^17.0.0
  smooth_page_indicator: ^1.2.1
  cloud_firestore: ^5.6.12
  path_provider: ^2.1.2
  http: ^1.2.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/asthmaguard-logo.svg
    - assets/images/
